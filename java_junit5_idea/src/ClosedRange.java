import java.util.logging.Logger;

public class ClosedRange {

    private static final Logger log = Logger.getLogger(ClosedRange.class.getName());

    public ClosedRange(int low, int up) {
        log.info("create ClosedRange");

    }
}
